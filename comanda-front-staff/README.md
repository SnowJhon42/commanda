# comanda-front-staff

Frontend staff (desktop-first) para `ADMIN`, `KITCHEN`, `BAR`, `WAITER`.

## Run

```bash
npm install
npm run dev
```

## Env

- `VITE_API_URL` (default `http://localhost:8000`)
